# runs/ — LEGACY (read-only)

This directory has been migrated to the workspace-scoped data layout.

**New location:** `data/workspaces/dev_default/runs/`

All run artifacts (candidate_pool.jsonl, run_summary.md, raw_jds/, etc.) are
preserved at the new location. This directory is kept for reference only.
New runs will be created under `data/workspaces/dev_default/runs/`.

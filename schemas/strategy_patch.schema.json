{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "strategy_patch.schema.json",
  "title": "StrategyPatch",
  "description": "Cross-run learnings produced by the bounded career-reflect-agent after a discovery run. The agent writes this file only; the worker validates it (strategy_state.apply_strategy_patch) and is the sole writer of strategy_state.json. List fields are merged (union, deduped); recommended_next_searches is replaced; coverage_by_workstream is merged per key. Unknown fields are rejected.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "effective_sources": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Sources that produced real JD URLs (domain or source type)."
    },
    "avoid_sources": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Sources to down-weight, each with a failure reason (403 / 404 / bot-blocked / login-required)."
    },
    "effective_query_patterns": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Query patterns that yielded real job-posting URLs."
    },
    "avoid_query_patterns": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Query patterns that only returned aggregator / search-result pages."
    },
    "coverage_by_workstream": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "enum": ["sufficient", "weak", "missing", "unknown"]
      },
      "description": "Per-workstream coverage assessment, keyed by a label from configs/workstream_taxonomy.yaml."
    },
    "key_learnings": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Free-text learnings to accumulate across runs."
    },
    "recommended_next_searches": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Priority directions for the next run (replaces the previous value)."
    }
  }
}
